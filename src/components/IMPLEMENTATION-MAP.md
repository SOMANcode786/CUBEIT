# Requested Improvement Map

1. **Viewport fixed until animation completion**
   - `Growth Engine`: GSAP ScrollTrigger pin with progress-driven stages.
   - `CubeIT + CubeIQ`: GSAP ScrollTrigger pin with cloth-reel progress.
   - Mobile and reduced-motion modes deliberately disable long pinning.

2. **Black/empty spaces**
   - Removed the former `720vh` engine spacer.
   - Added explicit opaque theme-aware surfaces to every major section.
   - Pin spacing is generated by ScrollTrigger rather than arbitrary CSS height.

3. **Cloth reel**
   - Added `ClothReel.tsx` using a subdivided Three.js plane, video texture, scroll velocity and pinned-corner deformation.
   - Poster fallback prevents blank output when video is unavailable.

4. **SVG importance**
   - Existing platform SVGs appear in the tools ecosystem, engine inputs and CubeIQ side of the relationship story.
   - New custom growth-path and relationship connector SVGs are progress-driven.

5. **Navbar root routes**
   - `CubeIQNavbar.tsx` preserves the global navbar while converting local hashes into root-page hash routes.

6. **Growth engine redesign**
   - Replaced orbit/word-wheel design with a flowing channel → system → result architecture.
   - Ten detailed operational actions are grouped into five understandable commercial stages.

7. **Heading scale and animation**
   - Reduced global headline sizes.
   - Added line-mask, perspective, blur-to-sharp and accent-sweep animation.

8. **Less overwhelming copy**
   - Shortened service and deliverable descriptions.
   - Only one growth-engine stage is visible at a time.
   - Relationship story reveals three concise phases instead of displaying all explanation simultaneously.
